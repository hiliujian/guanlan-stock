# ---- Logs ----
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# ---- Dependencies ----
node_modules
.DS_Store
*.local

# ---- Local env (含 Supabase anon key，虽前端可公开但勿入库) ----
.env
.env.*
!.env.example

# ---- Build outputs (NOT project source) ----
dist
dist_bak_old
dist_old_*
h5out/
h5out*/

# ---- Temp / scratch / library reference copies (each has its own .git) ----
_s.html
_v.html
test_ucharts.mjs
selftest
_ucharts_src
_uniui_src

# ---- Local preview server & auxiliary helpers (not part of the uni-app build) ----
# 注意：server/（行情代理+静态服务）与 supabase/（DB schema + Edge Function）属于「后端」，
# 已按需求纳入版本库（前后端同仓）；仅 webapp（旧构建产物）仍排除。
webapp

# ---- 本地临时 / scratch / 开发期产物（不入库） ----
Microsoft/
dist_trash_*/
tmp/
supabase/.temp/

# ---- Editor directories and files ----
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
